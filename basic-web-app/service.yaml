apiVersion: v1
kind: Service
metadata:
  name: simple-webapp
  labels:
    app: simple-webapp
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: simple-webapp

